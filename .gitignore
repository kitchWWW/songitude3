# macOS
.DS_Store

# Node (Lambda deps are restored with `npm install`)
**/node_modules/

# Xcode
**/DerivedData/
**/build/
**/xcuserdata/
*.xcuserstate

# Large sample bundle — exceeds GitHub's 100 MB file limit; regenerate or re-add locally.
ios/Songitude/Experiences/magic_square.zip

# Scratch
*.log

# Demo videos for App Review — over GitHub's 100 MB file limit; kept locally only.
docs/demo-videos/*.mp4

# Android
android/local.properties
android/keystore.properties
android/.gradle/
android/build/
android/app/build/
android/.kotlin/
__pycache__/
