<resources>
    <style name="Theme.Songitude" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Android 12+ runs its own splash screen and ignores windowBackground for it, so the same
         picture is described in its vocabulary: the wash's centre colour, and the mark as the icon
         at the size the animation opens on. -->
    <style name="Theme.Songitude.Launch" parent="Theme.Songitude">
        <item name="android:windowBackground">@drawable/launch_splash</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_mark</item>
    </style>
</resources>
